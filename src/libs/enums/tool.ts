export enum ToolType {
  NODE = "Node",
  BUN = "Bun",
  DENO = "Deno",
}

export enum VersionType {
  LTS = "LTS",
  CURRENT = "Current",
  STABLE = "Old Stable",
  UNSTABLE = "Old Unstable",
  DEFAULT = "Default",
}
